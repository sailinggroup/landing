{
  "name": "sail-frontend",
  "version": "0.0.1",
  "private": true,
  "description": "Sail — frontend (Vite + Solid + TypeScript)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "solid-js": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "vite": "^5.0.0",
    "vite-plugin-solid": "^2.10.0"
  }
}
